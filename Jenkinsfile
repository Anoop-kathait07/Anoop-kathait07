pipeline {
    agent any
        stages {
            stage (first stage build ){
                steps {
                    sh 'echo "Hello word this is a test project"'
                }
            }
        }
}
